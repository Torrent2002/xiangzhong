# 测试依赖（开发期，非运行时）
# 安装：pip install -r requirements-dev.txt
# 国内若 SSL 失败，用阿里云 HTTP 镜像：
#   pip install -i http://mirrors.aliyun.com/pypi/simple --trusted-host mirrors.aliyun.com -r requirements-dev.txt
pytest>=7
